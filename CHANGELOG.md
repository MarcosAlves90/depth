# Changelog

This project follows the [Keep a Changelog](https://keepachangelog.com/)
format.

## [Unreleased]

### Added

- user-provided search items through repeatable `--pattern`/`--item` options;
- complete JSON artifact through `--output`, using schema `depth.scan.v1`;
- local setup, architecture, and testing documentation;
- modular Python scanner for current trees and Git history;
- evidence by commit, branch, tag, file, line, and parent snapshot;
- initial unit and integration test suite.

### Removed

- original Bash implementation.
